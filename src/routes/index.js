import { NavigationContainer } from '@react-navigation/native';
import { Stack } from '~/routes/stack';

export const Routes = () => {
  return (
    <NavigationContainer>
      <Stack />
    </NavigationContainer>
  );
};
